{% extends 'base.html.twig' %}

{% block title %}Bibliothecaire{% endblock %}

{% block body %}
    <h1>Bibliothecaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bibliothecaire.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ bibliothecaire.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ bibliothecaire.prenom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ bibliothecaire.adresse }}</td>
            </tr>
            <tr>
                <th>Numtel</th>
                <td>{{ bibliothecaire.numtel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bibliothecaire_index') }}">back to list</a>

    <a href="{{ path('app_bibliothecaire_edit', {'id': bibliothecaire.id}) }}">edit</a>

    {{ include('bibliothecaire/_delete_form.html.twig') }}
{% endblock %}
